#ifndef LQC_UTILS_CAMERA_H
#define LQC_UTILS_CAMERA_H

#include <math/Vector3.h>
#include <lqc/lqcdefines.h>

namespace lqc
{
  /*class Camera
  {
  public:
    Camera ();
    ~Camera ();

    void Move (float forward, float up, float right);
    void Rotate (float axis_x, float axis_y);

  private:
    Vector3f m_f, m_u, m_r;
    Vector3f m_p;
    float m_speed;
    float m_rot_sensivity;

    Vector3f m_total_rotate;
  };*/
}

#endif